version: "3"
services:

  # ===========================
  # main image being tested
  # ===========================
  cowbird:
    container_name: "cowbird-smoke-test"
    image: pavics/cowbird:latest
    restart: "no"
    env_file:
      - test.env
    # FIXME: other details?
